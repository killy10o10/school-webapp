function CustomTheme() {
  const theme = {
    tab: {
      styles: {
        base: {
          indicator: {
            bg: "bg-blue-500",
          },
        },
      },
    },
  };
  return theme
}

export default CustomTheme
